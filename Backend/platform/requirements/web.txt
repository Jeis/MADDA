# Spatial Platform - Web Requirements
# Authentication, HTTP clients, and WebSocket support

# HTTP client libraries
requests>=2.31.0,<3.0.0

# WebSocket support for real-time features
websockets>=12.0,<15.0.0

# Authentication and security stack
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
pyjwt>=2.8.0,<3.0.0
cryptography>=41.0.7,<45.0.0

# Redis caching (compatible with redis:7-alpine)
redis[hiredis]>=5.0.0,<6.0.0