# Spatial Platform - Web Requirements
# Authentication, HTTP clients, and WebSocket support

# HTTP client libraries (SECURITY UPDATED)
requests>=2.32.3,<3.0.0  # Security fixes for certificate handling

# WebSocket support for real-time features (UPDATED)
websockets>=13.1,<15.0.0  # Latest stable with protocol improvements

# Authentication and security stack (SECURITY UPDATED)
python-jose[cryptography]>=3.3.0,<4.0.0  # Still maintained, no critical issues
passlib[bcrypt]>=1.7.4,<2.0.0  # Still secure
pyjwt>=2.10.1,<3.0.0  # Security improvements and bug fixes
cryptography>=43.0.3,<45.0.0  # Critical security updates

# Redis caching (compatible with redis:7-alpine) (UPDATED)
redis[hiredis]>=5.2.1,<6.0.0  # Latest stable with bug fixes