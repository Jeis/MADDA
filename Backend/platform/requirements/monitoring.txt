# Spatial Platform - Monitoring Requirements
# Enterprise observability stack - PRESERVING ALL TELEMETRY

# Core metrics and monitoring (UPDATED)
prometheus-client>=0.21.1,<1.0.0  # Latest stable
sentry-sdk>=2.18.0,<3.0.0  # Major performance improvements

# OpenTelemetry instrumentation (CRITICAL - UPDATED)
opentelemetry-api>=1.29.0,<2.0.0  # Latest stable API
opentelemetry-sdk>=1.29.0,<2.0.0  # Latest stable SDK
opentelemetry-instrumentation>=0.50b0,<1.0.0  # Latest instrumentation

# Service instrumentation (CRITICAL - UPDATED)
opentelemetry-instrumentation-fastapi>=0.50b0,<1.0.0  # Compatible with FastAPI 0.115+
opentelemetry-instrumentation-requests>=0.50b0,<1.0.0  # Latest stable
opentelemetry-instrumentation-psycopg2>=0.50b0,<1.0.0  # Database tracing
opentelemetry-instrumentation-redis>=0.50b0,<1.0.0  # Redis tracing

# Exporters (CRITICAL - UPDATED)
opentelemetry-exporter-otlp>=1.29.0,<2.0.0  # Latest OTLP exporter
opentelemetry-exporter-jaeger>=1.29.0,<2.0.0  # Latest Jaeger exporter
opentelemetry-exporter-prometheus>=0.50b0,<1.0.0  # Latest Prometheus exporter

# Propagators (CRITICAL - UPDATED)
opentelemetry-propagator-b3>=1.29.0,<2.0.0  # Latest B3 propagator
opentelemetry-propagator-jaeger>=1.29.0,<2.0.0  # Latest Jaeger propagator